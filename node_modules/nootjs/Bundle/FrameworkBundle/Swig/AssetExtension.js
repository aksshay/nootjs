var extension = function(swig, kernel)
{
    this.swig = swig;
    this.kernel = kernel;

    this.addFilters = function()
    {
        // Asset filter
        this.swig.setFilter("asset", function(path){
            return kernel.getBaseUrl() + path;
        });
    }
}

module.exports = extension;